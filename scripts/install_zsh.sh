#!/bin/bash
echo "Install Zsh using package manager"

# INSTALL_DIR="$HOME/.local/bin"
#
# sh -c "$(curl -fsSL https://raw.githubusercontent.com/romkatv/zsh-bin/master/install)" "" \
#     -d "$INSTALL_DIR" \
#     -e no
#
# echo "✅ Zsh is installed as $INSTALL_DIR/bin/zsh"
